spring:
  application:
    name: CLOUD-CONSUMER-USER-FEIGN-ACTUATOR
server:
  port: 8006
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true #注册时使用ip的方式
logging:
  level:
    com.liquor.cloud.consumer.user.feign.MovieServiceFeign: debug
